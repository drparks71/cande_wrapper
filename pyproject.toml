[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "cande-wrapper"
version = "0.0.3"
description = "Placeholder for the CANDE FEA culvert analysis engine wrapper"
requires-python = ">=3.10"